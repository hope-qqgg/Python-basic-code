"""
==========================
  练习 09:函数基础实战
  用到的知识:def 定义、参数、返回值、多返回值、函数调用
  难度:★★★☆☆
  规则:每道题先自己思考,卡住再看题下的【小提示】
==========================
"""

# ------------------------------------------------------------
# 题目 1:打招呼函数
# 定义一个函数 welcome(),函数体里打印 "欢迎来到Python世界!"
# 然后调用 2 次
# ------------------------------------------------------------
# 【小提示】
# def welcome(): + 缩进的函数体
# 调用:函数名加括号 welcome()
# 函数定义后不会执行,只有调用才会执行!

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 2:矩形面积函数
# 定义一个函数 rect_area(w, h),接收宽和高,返回面积
# 调用并输出:5 × 8 的矩形面积是:40
# ------------------------------------------------------------
# 【小提示】
# def rect_area(w, h):
#     return w * h
# 调用:rect_area(5, 8),用 f-string 打印结果

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 3:判断偶数函数
# 定义一个函数 is_even(n),n 是偶数返回 True,否则返回 False
# 测试:print(is_even(4)) → True;print(is_even(7)) → False
# ------------------------------------------------------------
# 【小提示】
# return n % 2 == 0 一行搞定!
# % 是取余,偶数除以 2 余数为 0

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 4:多返回值函数
# 定义一个函数 score_info(a, b, c),接收 3 科成绩
# 返回 (总分, 平均分保留1位)
# 调用 score_info(85, 92, 78) 并输出结果
# ------------------------------------------------------------
# 【小提示】
# 返回多个值用逗号分隔:return total, avg
# 接收时也要多个变量:t, a = score_info(85, 92, 78)
# 保留1位:round(x, 1)

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 5:综合挑战:成绩统计函数
# 定义一个函数 stats(scores),接收一个成绩列表
# 返回 (最高分, 最低分, 平均分保留1位)
# 测试:stats([85, 92, 78, 90, 88])
# 输出:(92, 78, 86.6)
# ------------------------------------------------------------
# 【小提示】
# max() / min() / sum() / len() 都是现成函数
# return max(scores), min(scores), round(sum(scores) / len(scores), 1)

# 您的代码写在这里 ↓↓↓
