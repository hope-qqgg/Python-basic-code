"""
==========================
  练习 11:函数高阶实战
  用到的知识:lambda 匿名函数、函数作为参数、递归
  难度:★★★★★
  规则:每道题先自己思考,卡住再看题下的【小提示】
==========================
"""

# ------------------------------------------------------------
# 题目 1:lambda 加法
# 用 lambda 定义 add = lambda x, y: x + y
# 调用 add(3, 5) 并输出
# ------------------------------------------------------------
# 【小提示】
# lambda 参数: 表达式  ← 一行函数,自动返回表达式结果
# 相当于 def add(x, y): return x + y

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 2:lambda + sort 排序
# words = ["Python", "Java", "Go", "JavaScript", "C"]
# 按"字符串长度"从小到大排序,输出结果
# ------------------------------------------------------------
# 【小提示】
# words.sort(key=lambda w: len(w))
# key 参数告诉 sort"按什么规则排"
# 结果是:['C', 'Go', 'Java', 'Python', 'JavaScript']

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 3:递归求和
# 用"递归"计算 1 + 2 + 3 + ... + n
# 定义 sum_n(n),测试 sum_n(100) → 5050
# ------------------------------------------------------------
# 【小提示】
# 递归 = 自己调用自己,必须有"终结点"
# sum_n(5) = 5 + sum_n(4)
# 终结点:if n == 1: return 1
# 完整:return n + sum_n(n - 1)

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 4:递归阶乘
# 用递归计算 n! (n的阶乘:1×2×3×...×n)
# 定义 factorial(n),测试 factorial(5) → 120
# ------------------------------------------------------------
# 【小提示】
# 和题目 3 几乎一样,把 + 换成 × 就行
# factorial(5) = 5 × 4 × 3 × 2 × 1 = 120
# 终结点:if n == 1: return 1

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 5:函数作为参数
# 定义 4 个函数:add/sub/mul/div(两个数加减乘除)
# 再定义 calc(x, y, oper),返回 oper(x, y)
# 测试:
#   calc(10, 5, add) → 15
#   calc(10, 5, sub) → 5
#   calc(10, 5, mul) → 50
#   calc(10, 5, div) → 2.0
# ------------------------------------------------------------
# 【小提示】
# 函数名不加括号传进去,就是"把函数当参数"
# calc 内部再调用 oper(x, y) 就执行了

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 6:综合挑战(力扣风格!)⚡
# 这是 LeetCode 第 1 题"两数之和"的简化版:
# 定义 two_sum(nums, target),从列表中找两个数,
# 它们的和等于 target,返回这两个数的下标(索引)。
# 测试:two_sum([2, 7, 11, 15], 9) → [0, 1]
#      (因为 nums[0] + nums[1] = 2 + 7 = 9)
# ------------------------------------------------------------
# 【小提示】
# 双重循环:外层 i 从 0 开始,内层 j 从 i+1 开始
# 检查 nums[i] + nums[j] == target,成立就 return [i, j]
# 找不到就 return []

# 您的代码写在这里 ↓↓↓
