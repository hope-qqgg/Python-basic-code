"""
==========================
  练习 10:函数进阶实战
  用到的知识:默认参数、关键字参数、*args、**kwargs、global
  难度:★★★★☆
  规则:每道题先自己思考,卡住再看题下的【小提示】
==========================
"""

# ------------------------------------------------------------
# 题目 1:默认参数
# 定义函数 register(name, age, gender="男", city="北京")
# 打印 f"注册成功:{name}, {age}岁, {gender}, {city}"
# 1. 调用 register("张三", 20)        ← 性别城市用默认值
# 2. 调用 register("李四", 18, "女", "上海")  ← 全部指定
# ------------------------------------------------------------
# 【小提示】
# 默认参数:调用时可以不传,自动使用默认值
# 注意:默认参数必须放在没有默认值的参数后面!

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 2:关键字参数
# 接着题目 1 的函数,用关键字参数调用:
# register(city="广州", name="王五", age=22, gender="男")
# 观察:顺序打乱也能对上号!
# ------------------------------------------------------------
# 【小提示】
# 关键字参数:调用时写 参数名=值,顺序无关紧要
# 位置参数在前,关键字参数在后

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 3:不定长位置参数 *args
# 定义函数 my_sum(*nums),返回所有参数的和
# 测试:my_sum(1, 2, 3) → 6;my_sum(10, 20, 30, 40, 50) → 150
# ------------------------------------------------------------
# 【小提示】
# *args 会收集所有"多出来的位置参数"成一个元组
# 里面就是 sum(nums)

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 4:不定长关键字参数 **kwargs
# 定义函数 show_info(**kwargs),遍历打印 "key: value"
# 测试:show_info(姓名="小明", 年龄=18, 城市="北京")
# ------------------------------------------------------------
# 【小提示】
# **kwargs 收集所有"关键字参数"成一个字典
# 遍历用 for k, v in kwargs.items()

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 5:global 修改全局变量
# 全局变量 count = 0
# 定义函数 add_count(),用 global 让 count 每次 +1
# 调用 3 次后打印 count(应该是 3)
# ------------------------------------------------------------
# 【小提示】
# 函数内默认只能读全局变量,不能改
# 想改必须声明:global count
# 试试不加 global 会怎样?会报错!

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 6:综合挑战:成绩统计函数(*args 版)
# 定义函数 calc_scores(*scores),接收任意多个成绩
# 返回 (最高分, 最低分, 平均分保留1位, 及格人数)
# 及格标准:>= 60
# 测试:calc_scores(85, 92, 78, 55, 90, 88)
# 输出:(92, 55, 81.3, 5)  ← 55 不及格,其余 5 个及格
# ------------------------------------------------------------
# 【小提示】
# 及格人数:用列表推导式 len([s for s in scores if s >= 60])
# 平均分:round(sum(scores) / len(scores), 1)

# 您的代码写在这里 ↓↓↓
