"""
==========================
  练习 12:模块与包实战
  用到的知识:import、from...import、别名、自定义模块
  难度:★★★★☆
  规则:本课需要您自己创建一个"模块文件",请先做准备工作
==========================

📌 准备工作(重要!):
  请在 练习/ 目录下新建一个文件,名字叫:my_tools.py
  (和本文件在同一级目录,即 D:\Code\Python_basic_code\basic-code\练习\my_tools.py)
  然后在这个 my_tools.py 里定义 3 个函数(只写函数,不用调用):
    1. c_to_f(c)    摄氏温度转华氏温度  公式:c * 9 / 5 + 32
    2. is_prime(n)  判断 n 是不是素数(质数),返回 True/False
    3. gcd(a, b)    计算 a 和 b 的最大公约数
  写完保存,再回到本文件做题!
"""

# ------------------------------------------------------------
# 题目 1:import 整个模块
# 用 import my_tools 导入您刚写的模块
# 然后调用:my_tools.c_to_f(100) → 212.0
#          my_tools.is_prime(7)  → True
#          my_tools.is_prime(8)  → False
#          my_tools.gcd(12, 18)  → 6
# 全部打印出来
# ------------------------------------------------------------
# 【小提示】
# import 模块名 → 调用:模块名.函数名()
# 模块要放在和本文件同一个目录,Python 才能找到

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 2:from ... import ... 导入指定函数
# 用 from my_tools import is_prime 只导入 is_prime
# 然后直接调用 is_prime(13) → True
# ------------------------------------------------------------
# 【小提示】
# from 模块 import 函数 → 调用时不用写模块名前缀
# 对比题目 1:import 全部 vs 只导入需要的

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 3:别名导入
# 用 import my_tools as mt 导入
# 然后调用 mt.c_to_f(37) → 98.6
#          mt.gcd(24, 36) → 12
# ------------------------------------------------------------
# 【小提示】
# as 别名:import 模块名 as 简称
# 名字太长时特别好用,比如 import random as rd

# 您的代码写在这里 ↓↓↓


# ------------------------------------------------------------
# 题目 4(自由挑战):给 my_tools.py 加一个函数
# 回到 my_tools.py,新加一个函数:
#   avg_score(scores)  接收成绩列表,返回平均分(保留1位)
# 然后在本文件里调用它,测试:avg_score([85, 92, 78]) → 85.0
# ------------------------------------------------------------
# 【小提示】
# 修改 my_tools.py 后,本文件要重新运行才会生效
# 调用方式:my_tools.avg_score([85, 92, 78])

# 您的代码写在这里 ↓↓↓
